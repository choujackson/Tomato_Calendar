/**
 * API 基础地址配置
 * 
 * ⚠️ 重要提示：
 * 1. 如果在真机上测试（Expo Go），localhost 无法工作，必须使用电脑的IP地址
 * 2. 如果在模拟器上测试，可以使用 localhost
 * 3. 确保手机和电脑在同一个WiFi网络下
 * 
 * 获取IP地址方法：
 * - Windows: 运行 ipconfig，查找 "IPv4 地址"
 * - Mac/Linux: 运行 ifconfig 或 ip addr
 */

// 方式1: 使用 localhost (仅适用于模拟器/浏览器)
// export const API_BASE_URL = 'http://localhost:3000';

// 方式2: 使用IP地址 (真机测试时必须使用)
// 请根据您的实际IP地址修改下面这行
// 根据检测到的IP，您当前的网络IP可能是：
// - 172.20.10.3 (可能是热点或特定网络)
// - 192.168.153.1 (局域网IP，通常用于WiFi)
// - 192.168.17.1 (局域网IP，通常用于WiFi)
// 
// 真机测试时，请确保：
// 1. 手机和电脑在同一个WiFi网络下
// 2. 使用电脑的局域网IP地址（通常是 192.168.x.x）
// 3. 如果使用热点，使用热点的IP地址（通常是 172.x.x.x 或 192.168.x.x）
// 
// 如果 192.168.153.1 不工作，请尝试 192.168.17.1 或 172.20.10.3
export const API_BASE_URL = 'http://172.20.10.3:3000';

